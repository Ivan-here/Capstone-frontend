import React from 'react';
import './SplashScreen.css';

const SplashScreen = () => {
    return (
        // Just an empty container now
        <div className="splash-container">
        </div>
    );
};

export default SplashScreen;